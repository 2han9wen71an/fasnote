source 'https://rubygems.org'
gem 'github-pages', group: :jekyll_plugins
gem 'json'

#gem 'jekyll-admin', group: :jekyll_plugins

gem "webrick", "~> 1.7"

# 添加Windows下的文件系统监视器，避免轮询
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# 添加并行处理器
gem 'parallel'

# 添加缓存插件
gem 'jekyll-include-cache'
